# Dart / Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
pubspec.lock

# Platform build artefacts
linux/
macos/
windows/

# IDE / Editor
.idea/
.vscode/
*.iml
*.suo
*.user
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Test coverage
coverage/

# JetVeil — generated static assets are committed to git so Vercel Lambda always
# has them available. Re-run `npm run build` in server/ after updating npm deps,
# then commit the updated files.

